language: ruby
services:
  - postgresql
rvm:
  - 2.3.1

before_script:
  - psql -c 'create database kingsly_test;' -U postgres
  - bundle exec rake db:create db:migrate
script:
  - bundle
  - bundle exec rspec
